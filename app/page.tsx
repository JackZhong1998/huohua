'use client';
import BubbleGarden from './BubbleGarden';
import MovieEditor from './MovieEditor';
import {getTopics,type Audience,type StoryFormat} from './story-data';
import {useEffect,useRef,useState} from 'react';
import {ArrowLeft,Check,ChevronRight,GitBranch,Heart,Home,MessageCircle,Mic,Pause,Play,Plus,RotateCcw,Share2,Sparkles,User,Volume2,VolumeX,X} from 'lucide-react';

type Line={at:number,text:string,accent?:string};
type Film={title:string,duration:number,kind:string,lines:Line[]};
const films:Record<string,Film>={
 hook:{title:'末日前一个月',duration:18,kind:'hook',lines:[{at:0,text:'你好。'},{at:1.7,text:'不要划走。'},{at:3.4,text:'这是第一集，',accent:'剧情钩子。'},{at:5.8,text:'想象一下——'},{at:7.2,text:'你穿越回',accent:'世界末日'},{at:9.5,text:'冰雪覆盖全球的',accent:'前一个月。'},{at:12,text:'你会做什么？'},{at:14.4,text:'好。'},{at:15.5,text:'等一下，进入下一页。'}]},
 intro:{title:'活下去',duration:29,kind:'intro',lines:[{at:0,text:'这是第二集。'},{at:2.1,text:'矛盾。紧迫。',accent:'还有希望。'},{at:4.8,text:'世界末日，',accent:'我怎么活下去？'},{at:8,text:'我没钱。'},{at:9.8,text:'好像活不下去。'},{at:12,text:'但我可以',accent:'无限网贷。'},{at:15,text:'而且——'},{at:16.7,text:'我还是',accent:'荒野求生专家。'},{at:20,text:'用户基本勾住了。'},{at:23,text:'但还不够。'},{at:25.5,text:'现在，',accent:'开始了解我们。'}]},
 team:{title:'了解团队',duration:27,kind:'team',lines:[{at:0,text:'我是',accent:'钟镇杰。'},{at:2.4,text:'两段跟随创业。'},{at:4.8,text:'一段孵化器',accent:'独立做互动小说。'},{at:8,text:'现在在剪映，'},{at:10,text:'负责剪辑、',accent:'AI 成片 Agent。'},{at:13.5,text:'团队里有——'},{at:15,text:'北影的 AI 产品。'},{at:17.5,text:'剪映资深技术。'},{at:20,text:'我们能设计',accent:'AI 创作工具，'},{at:23,text:'也能用 AI',accent:'做出作品。'}]},
 idea:{title:'了解 idea',duration:35,kind:'idea',lines:[{at:0,text:'我们在做——'},{at:2,text:'抖音、红果之外的',accent:'互动式视频平台。'},{at:5,text:'「火花」'},{at:7,text:'从互动影视游戏的',accent:'生产、消费平台开始。'},{at:11,text:'走向',accent:'无限视频消费平台。'},{at:14,text:'成熟的 LLM 与视频模型',accent:'带来两个变量。'},{at:19,text:'内容供给',accent:'低门槛化、规模化。'},{at:23,text:'解锁',accent:'无限流玩法。'},{at:27,text:'扩大供给。'},{at:29,text:'扩大消费时长。'},{at:31,text:'扩大可服务人群。'},{at:33,text:'打开新的内容市场。'}]},
 market:{title:'了解市场',duration:38,kind:'market',lines:[{at:0,text:'互动影游市场',accent:'约 18 亿元。'},{at:4,text:'短剧市场',accent:'约 678 亿元。'},{at:8,text:'预计 2030 年',accent:'约 1506 亿元。'},{at:12,text:'火花的市场路径——'},{at:14,text:'0—2 年',accent:'进入 18 亿互动影游市场。'},{at:18,text:'验证',accent:'内容付费。'},{at:21,text:'2—5 年',accent:'AI 渗透短剧大盘。'},{at:25,text:'占中国短剧市场',accent:'5%—10%。'},{at:28,text:'对应约',accent:'75—150 亿元市场。'},{at:32,text:'5 年后',accent:'扩展海外、知识、陪伴。'},{at:35,text:'走向全球',accent:'互动视频平台机会。'}]},
};
const runway=[{id:44557,title:'霓虹跑车',likes:'3.2万'},{id:40741,title:'复古锋芒',likes:'1.8万'},{id:44559,title:'镜中目光',likes:'2.4万'},{id:40656,title:'蓝调肖像',likes:'9,826'}];
const choices:Record<string,string[]>={intro:['了解团队','了解 idea','了解市场'],team:['了解 idea','了解市场','联系方式'],idea:['了解团队','了解市场','联系方式'],market:['了解团队','了解 idea','联系方式']};

export default function App(){
 const[page,setPage]=useState('home'),[story,setStory]=useState(false),[film,setFilm]=useState('hook'),[branch,setBranch]=useState(false),[timeline,setTimeline]=useState(false),[contact,setContact]=useState(false),[muted,setMuted]=useState(true),[paused,setPaused]=useState(false),[feedIndex,setFeedIndex]=useState(0),[path,setPath]=useState<string[]>([]),[playMode,setPlayMode]=useState<'game'|'watch'>('game'),[toast,setToast]=useState('');const feed=useRef<HTMLDivElement>(null),hookTime=useRef(0);
 useEffect(()=>{if(!toast)return;const t=setTimeout(()=>setToast(''),2200);return()=>clearTimeout(t)},[toast]);
 useEffect(()=>{if(page!=='home'||story)return;const buttons=document.querySelectorAll<HTMLButtonElement>('.feed-head button');const follow=buttons[1];if(!follow)return;const open=()=>setToast('商业计划书请联系团队');follow.addEventListener('click',open);return()=>follow.removeEventListener('click',open)},[page,story]);
 function enter(ended=false){setStory(true);setFilm(ended?'intro':'hook');setBranch(false);setPaused(false)}
 function ended(){if(film==='hook'){setFilm('intro');setBranch(false)}else setBranch(true)}
 function pick(v:string){if(v==='联系方式'){setContact(true);setBranch(false);return}setPath(p=>[...p,v]);setFilm(v==='了解团队'?'team':v==='了解 idea'?'idea':'market');setBranch(false);setPaused(false)}
 return <main className="stage"><div className="app-shell">
  {page==='home'?<section className="home-page">{!story?<><div className="feed-head"><Logo/><div><button className="active">推荐</button><button>关注</button></div></div><div className="video-feed" ref={feed} onScroll={()=>{const n=feed.current;if(n)setFeedIndex(Math.round(n.scrollTop/n.clientHeight))}}><article className="feed-item"><Kinetic film={films.hook} active={feedIndex===0} muted={muted} paused={paused} startAt={0} onProgress={n=>{hookTime.current=n}} onEnded={()=>enter(true)} onTap={()=>setPaused(v=>!v)}/><FeedUI title="世界末日前一个月" likes="5.8万" enter={()=>enter(false)} muted={muted} setMuted={setMuted}/></article>{runway.map((r,i)=><article className="feed-item" key={r.id}><Native id={r.id} active={feedIndex===i+1} muted={muted} paused={paused} onEnded={()=>enter(true)} onTap={()=>setPaused(v=>!v)}/><FeedUI title={r.title} likes={r.likes} enter={()=>enter(true)} muted={muted} setMuted={setMuted}/></article>)}</div></>:<div className="story-player" key={film}>{contact?<Contact back={()=>setContact(false)}/>:<><Kinetic film={films[film]} active={!timeline} muted={muted} paused={paused||branch} startAt={film==='hook'?hookTime.current:0} onEnded={ended} onTap={()=>!branch&&setPaused(v=>!v)}/><div className="story-top"><button aria-label="返回首页" onClick={()=>{setStory(false);setFilm('hook');setBranch(false)}}><ArrowLeft/></button><span>{films[film].title}<small>{film==='hook'?'第一集':film==='intro'?'第二集':'互动分支'}</small></span><button aria-label="分支总览" onClick={()=>setTimeline(true)}><GitBranch/></button></div>{branch&&<Choice title={film==='intro'?'开始了解我们':films[film].title} items={choices[film]||choices.intro} pick={pick}/>}</>}{timeline&&<Timeline path={path} current={films[film].title} close={()=>setTimeline(false)} pick={pick} mode={playMode} setMode={setPlayMode}/>}</div>}</section>:page==='create'?<Creator notify={setToast}/>:<Profile go={()=>{setPage('home');enter(false)}}/>}
  <nav className="bottom-nav"><button className={page==='home'?'active':''} onClick={()=>setPage('home')}><Home/><span>首页</span></button><button className={page==='create'?'active':''} onClick={()=>setPage('create')}><span className="create-mark"><Plus/></span><span>创作</span></button><button className={page==='me'?'active':''} onClick={()=>setPage('me')}><User/><span>我的</span></button></nav>
 </div>{toast&&<div className="toast"><Check/>{toast}</div>}</main>
}

function Logo(){return <span className="spark-logo">火花<i/></span>}
function Kinetic({film,active,muted,paused,startAt,onProgress,onEnded,onTap}:{film:Film,active:boolean,muted:boolean,paused:boolean,startAt:number,onProgress?:(n:number)=>void,onEnded:()=>void,onTap:()=>void}){const[time,setTime]=useState(startAt),audio=useRef<HTMLAudioElement>(null),done=useRef(false),timeRef=useRef(time);timeRef.current=time;useEffect(()=>{setTime(startAt);done.current=false},[film.title,startAt]);useEffect(()=>{const a=audio.current;if(!a)return;a.muted=muted;if(active&&!paused){a.currentTime=time%12;a.play().catch(()=>{})}else a.pause()},[active,paused,muted]);useEffect(()=>{if(!active||paused)return;let last=performance.now(),raf=0;const tick=(now:number)=>{const next=Math.min(film.duration,timeRef.current+(now-last)/1000);last=now;setTime(next);onProgress?.(next);if(next>=film.duration&&!done.current){done.current=true;onEnded();return}raf=requestAnimationFrame(tick)};raf=requestAnimationFrame(tick);return()=>cancelAnimationFrame(raf)},[active,paused,film.duration,onEnded,onProgress]);const line=[...film.lines].reverse().find(l=>time>=l.at)||film.lines[0],i=film.lines.indexOf(line);return <div className={`kinetic-film theme-${film.kind}`} onClick={onTap}><audio ref={audio} src="media/spark-beat.wav" loop/><div className="ice-field"><i/><i/><i/><i/></div><div className="kinetic-number">{String(i+1).padStart(2,'0')}</div><div className="beat-flash" key={i}/><div className="kinetic-copy" key={`${film.title}-${i}`}><span>{line.text}</span>{line.accent&&<strong>{line.accent}</strong>}</div><div className="film-meta"><span>火花原创演示</span><i/><span>{film.title}</span></div>{paused&&<span className="paused-icon"><Play fill="currentColor"/></span>}</div>}
function Native({id,active,muted,paused,onEnded,onTap}:{id:number,active:boolean,muted:boolean,paused:boolean,onEnded:()=>void,onTap:()=>void}){const ref=useRef<HTMLVideoElement>(null);useEffect(()=>{if(!ref.current)return;if(active&&!paused)ref.current.play().catch(()=>{});else ref.current.pause()},[active,paused,id]);return <div className="native-video" onClick={onTap}><video ref={ref} src={`media/${id}.mp4`} poster={`media/${id}.jpg`} muted={muted} playsInline onEnded={onEnded}/></div>}
function FeedUI({title,likes,enter,muted,setMuted}:{title:string,likes:string,enter:()=>void,muted:boolean,setMuted:(v:boolean)=>void}){const[like,setLike]=useState(false);return <><div className="feed-actions"><button aria-label="喜欢" onClick={()=>setLike(!like)} className={like?'liked':''}><Heart fill={like?'currentColor':'none'}/><small>{likes}</small></button><button aria-label="分享"><Share2/><small>分享</small></button></div><div className="feed-caption"><b>@火花放映室</b><h2>{title}</h2><p>点击下方进入完整互动故事</p></div><button aria-label={muted?'打开声音':'关闭声音'} className="sound-button" onClick={()=>setMuted(!muted)}>{muted?<VolumeX/>:<Volume2/>}</button><button className="feed-enter" onClick={enter}><Play fill="currentColor"/><span>进入故事</span><ChevronRight/></button></>}
function Choice({title,items,pick}:{title:string,items:string[],pick:(v:string)=>void}){return <div className="choice-overlay"><div><span>故事交给你</span><h2>{title}</h2></div>{items.map((x,i)=><button key={x} onClick={()=>pick(x)}><span>0{i+1}</span><b>{x}</b><ChevronRight/></button>)}</div>}
function Timeline({path,current,close,pick,mode,setMode}:{path:string[],current:string,close:()=>void,pick:(v:string)=>void,mode:'game'|'watch',setMode:(mode:'game'|'watch')=>void}){const history=path.at(-1)===current?path.slice(0,-1):path;return <div className="timeline-overlay"><header><div><span>你的时间线</span><h2>每一次了解，都更接近答案</h2></div><button aria-label="关闭分支总览" onClick={close}><X/></button></header><button className={`mode-toggle ${mode==='watch'?'watch':''}`} role="switch" aria-checked={mode==='watch'} aria-label="游戏模式与看剧模式切换" onClick={()=>setMode(mode==='game'?'watch':'game')}><span className={mode==='game'?'active':''}>游戏模式</span><i><b/></i><span className={mode==='watch'?'active':''}>看剧模式</span></button><div className="timeline-tree"><div className="node done"><Check/>末日前一个月</div><i/><div className="node done"><Check/>活下去</div>{history.map((p,i)=><span key={`${p}-${i}`}><i/><div className="node done"><Check/>{p}</div></span>)}<i/><div className="node current"><Sparkles/>{current}</div><i/><div className="branches">{['了解团队','了解 idea','了解市场'].map(x=><button key={x} onClick={()=>{close();pick(x)}}><GitBranch/>{x}</button>)}</div></div></div>}
function Contact({back}:{back:()=>void}){const[copied,setCopied]=useState(false);async function copy(){await navigator.clipboard?.writeText('lifetour2026');setCopied(true)}return <div className="contact-page"><button aria-label="返回互动视频" onClick={back}><ArrowLeft/></button><div className="contact-center"><img src="team-avatar.jpg" alt="钟镇杰"/><h2>钟镇杰</h2><p><MessageCircle/>微信号</p><div className="wechat-line"><strong>lifetour2026</strong><button onClick={copy}>{copied?'已复制':'复制'}</button></div></div></div>}

function Creator({notify}:{notify:(s:string)=>void}){const[view,setView]=useState('select'),[audience,setAudience]=useState<Audience>('女性向'),[format,setFormat]=useState<StoryFormat>('互动游戏'),[selected,setSelected]=useState<string[]>(['女性向','互动游戏']),[trail,setTrail]=useState<string[]>([]),[topicPage,setTopicPage]=useState(0);const topics=getTopics(audience,format);if(view==='editor')return <MovieEditor back={()=>setView('assets')} notify={notify}/>;if(view==='assets')return <section className="creator-page assets-page"><header><button onClick={()=>setView('select')}><ArrowLeft/></button><div><span>故事与角色</span><small>冰封之前</small></div><button onClick={()=>setView('editor')}>生成</button></header><div className="story-module"><span>故事概述</span><h2>冰封之前</h2><p>穿越回末日前一个月的荒野专家，决定利用无限网贷建立最后一座庇护所。</p><div><b>核心爽点</b><span>囤货升级</span><span>倒计时</span><span>身份反转</span></div></div><div className="character-module"><img src="story.png" alt="主角林晚"/><div><span>主角</span><h3>林晚</h3><p>荒野求生专家 · 冷静 · 行动力极强</p><button onClick={()=>notify('已切换角色方案')}><RotateCcw/>换个形象</button></div></div><button className="generate-cta" onClick={()=>setView('editor')}><Sparkles/>生成序章与主线</button></section>;
 const signal=trail.reduce((total,label)=>total+[...label].reduce((n,char)=>n+char.charCodeAt(0),0),0);const offset=(topicPage*3+signal)%topics.length;const shown=[0,1,2].map(i=>topics[(offset+i)%topics.length]);return <section className="creator-page selection-page"><header><Logo/><h1>创造故事</h1><span/></header><div className="fixed-picks"><div>{(['女性向','男性向'] as Audience[]).map(x=><button className={audience===x?'selected':''} key={x} onClick={()=>{setAudience(x);setSelected([x,format]);setTrail([]);setTopicPage(0)}}>{x}</button>)}</div><div>{(['互动游戏','短剧'] as StoryFormat[]).map(x=><button className={format===x?'selected':''} key={x} onClick={()=>{setFormat(x);setSelected([audience,x]);setTrail([]);setTopicPage(0)}}>{x}</button>)}</div></div><BubbleGarden audience={audience} format={format} onChange={labels=>{setTrail(labels);setSelected([audience,format,...labels]);setTopicPage(0)}}/><div className="topic-section"><div className="topic-head"><span>为你生成 · {selected.length} 个线索</span><button aria-label="换一组选题" onClick={()=>setTopicPage(p=>(p+1)%Math.ceil(topics.length/3))}><RotateCcw/>换一批</button></div>{shown.map((t,i)=><button key={t.title} onClick={()=>setView('assets')}><span>{String((offset+i)%topics.length+1).padStart(2,'0')}</span><div><h3>{t.title}</h3><p>{t.hook}</p></div><ChevronRight/></button>)}</div></section>}

function Editor({back,notify}:{back:()=>void,notify:(s:string)=>void}){const[playing,setPlaying]=useState(true),[selected,setSelected]=useState(0),[holding,setHolding]=useState(false);const timer=useRef<ReturnType<typeof setTimeout>|null>(null),scrollTimer=useRef<ReturnType<typeof setTimeout>|null>(null),holdingRef=useRef(false),track=useRef<HTMLDivElement>(null),video=useRef<HTMLVideoElement>(null),seekTime=useRef(0);useEffect(()=>{const v=video.current;if(v){if(playing)v.play().catch(()=>{});else v.pause()}},[playing,selected]);const ids=[44557,40741,1594,49878,40656,44559],names=['序章','采购物资','暴雪预警','庇护所','神秘来客','分支 A'];function sync(){setPlaying(true);if(scrollTimer.current)clearTimeout(scrollTimer.current);scrollTimer.current=setTimeout(()=>{const el=track.current;if(!el)return;const rect=el.getBoundingClientRect();const center=rect.left+rect.width/2;const clips=[...el.children] as HTMLElement[];let nearest=0,distance=Infinity;clips.forEach((clip,i)=>{const r=clip.getBoundingClientRect();const d=center<r.left?r.left-center:center>r.right?center-r.right:0;if(d<distance){distance=d;nearest=i}});const r=clips[nearest].getBoundingClientRect();seekTime.current=Math.max(0,Math.min(4.95,(center-r.left)/r.width*5));setSelected(nearest);const v=video.current;if(v&&v.getAttribute('src')===`/media/${ids[nearest]}.mp4`){v.currentTime=Math.min(seekTime.current,Number.isFinite(v.duration)?v.duration-.05:5);v.play().catch(()=>{})}setPlaying(true)},90)}function release(){if(timer.current)clearTimeout(timer.current);if(holdingRef.current){holdingRef.current=false;setHolding(false);notify('语音修改已记录')}}return <section className="editor-page"><div className="native-video" onClick={()=>setPlaying(!playing)}><video ref={video} src={`/media/${ids[selected]}.mp4`} muted playsInline loop onLoadedMetadata={()=>{const v=video.current;if(v){v.currentTime=Math.min(seekTime.current,v.duration-.05);if(playing)v.play().catch(()=>{})}}}/></div><div className="editor-shade"/><header><button aria-label="返回故事与角色" onClick={back}><ArrowLeft/></button><span>冰封之前<small>自动保存</small></span><button onClick={()=>notify('正在导出影片')}>导出</button></header><div className="editor-control"><div className="playhead"><i/></div><div aria-label="视频时间线" className="clip-track" ref={track} tabIndex={0} onScroll={sync}>{names.map((n,i)=><button className={selected===i?'selected':''} key={n} onClick={()=>{setSelected(i);setPlaying(true);track.current?.children[i]?.scrollIntoView({inline:'center',behavior:'smooth'})}}><img src={`/media/${ids[i]}.jpg`} alt=""/><span>{n}</span><small>{i===5?'待生成':'00:05'}</small></button>)}</div><button aria-label={playing?'暂停':'播放'} className="editor-play" onClick={()=>setPlaying(!playing)}>{playing?<Pause fill="currentColor"/>:<Play fill="currentColor"/>}</button></div><div className="editor-bottom"><div className="suggestions"><button onClick={()=>notify('正在重新生成当前片段')}><RotateCcw/>重新生成</button>{['更紧张','增加特写','冰雪更大','换成夜景','加强对白','镜头加速','人物沉默'].map(x=><button key={x} onClick={()=>notify(`已应用：${x}`)}>{x}</button>)}</div><button aria-label="长按录音" className={`voice-button ${holding?'holding':''}`} onPointerDown={()=>{timer.current=setTimeout(()=>{holdingRef.current=true;setHolding(true)},280)}} onPointerUp={release} onPointerCancel={release}><Mic/>{holding&&<span>松开发送</span>}</button></div></section>}
function Profile({go}:{go:()=>void}){return <section className="profile-page"><header><Logo/><h1>我的</h1><span/></header><div className="profile-card"><div className="contact-avatar small">钟</div><div><h2>钟镇杰</h2><p>火花发起人</p></div></div><div className="profile-stats"><div><span>创作额度</span><b>1,000</b></div><div><span>互动记录</span><b>4</b></div><div><span>作品</span><b>1</b></div></div><button className="recent-card" onClick={go}><div className="mini-kinetic">末日前<br/>一个月</div><div><span>最近看过</span><h3>冰封之前</h3><p>继续第一集</p></div><ChevronRight/></button></section>}
